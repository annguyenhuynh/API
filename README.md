# API
Python REST API - Tech with Tim & Code with Josh

* 💬 API represents the communication between the clients and the server 

* In technical terms, it's the way that the front end communicate with the backend (the database)

* Using **flask-sqlalchemy**, the db. as object is comoon (i.e. db.Column, db.Integer)